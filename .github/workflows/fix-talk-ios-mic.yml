name: Fix Talk iOS microphone

on:
  push:
    paths:
      - 'scripts/fix-talk-ios-mic.mjs'
      - '.github/workflows/fix-talk-ios-mic.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply Talk microphone fix
        run: node scripts/fix-talk-ios-mic.mjs
      - name: Install
        run: npm install
      - name: Trust tests
        run: npm run test:trust
      - name: Production build
        run: npm run build
      - name: Commit source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/TalkForMeRealtime.jsx
          git diff --cached --quiet && exit 0
          git commit -m "Keep Talk microphone start attached to user tap"
          git push
