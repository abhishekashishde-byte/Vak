import fs from 'node:fs'

const appPath = 'src/App.jsx'
const apiPath = 'api/translate.js'
let app = fs.readFileSync(appPath, 'utf8')
let api = fs.readFileSync(apiPath, 'utf8')

function replaceOrThrow(source, from, to, label) {
  if (!source.includes(from)) throw new Error(`Could not find ${label}`)
  return source.replace(from, to)
}

const oldPrompt = `      instructions = \`You are Ana, a premium translation engine. Detect the source language and translate into \${outputTarget}. Return ONLY the finished translation with no explanation, labels or quotation marks. Preserve paragraph breaks, line breaks, bullets, names, dates, numbers, URLs, greetings and signatures. Translate idiomatically and naturally, not word-for-word. Preserve the user's tone, intent and level of formality.\``
const newPrompt = `      instructions = \`You are Ana, a premium translation engine. Translation quality is the primary goal. Detect the source language and translate into \${outputTarget}. Return ONLY the finished translation with no explanation, labels or quotation marks. Preserve paragraph breaks, line breaks, bullets, names, dates, numbers, URLs, greetings and signatures. First understand the meaning of each sentence and paragraph, including intent, relationships between clauses, idioms, implied meaning, domain terminology and level of formality. Then write the message the way a fluent native speaker of \${outputTarget} would naturally express the SAME meaning. Do not mirror source-language word order, syntax or collocations when they sound unnatural in the target language. Prefer natural target-language phrasing over literal word substitution. Preserve every factual claim, request, condition, degree of certainty and emotional tone; do not add or remove meaning. If the source is awkward or non-native, translate the intended meaning rather than reproducing the awkward grammar. If the source is ambiguous, preserve the ambiguity instead of guessing. Keep terminology consistent across the whole text. Before returning the result, silently check it for semantic fidelity, naturalness, grammar, idiomatic phrasing and terminology consistency, then output only the polished translation.\``
app = replaceOrThrow(app, oldPrompt, newPrompt, 'translation quality prompt')

app = replaceOrThrow(
  app,
  `  const inputRef = useRef(null)\n  const dictationPositionRef = useRef(null)`,
  `  const inputRef = useRef(null)\n  const refinementCacheRef = useRef(new Map())\n  const dictationPositionRef = useRef(null)`,
  'refinement cache ref',
)

const oldInspect = `  const inspectWord = async (word, start, end) => {\n    if (!output || suggestLoading) return\n    setSelected({ word, start, end, sourceTerm: '', partOfSpeech: '', meaning: '', alternatives: [] })\n    setSuggestLoading(true)\n    try {\n      const prompt = \`SOURCE TEXT:\\n\${input}\\n\\nCURRENT \${target.toUpperCase()} TRANSLATION:\\n\${output}\\n\\nSELECTED TARGET WORD:\\n\${word}\\n\\nIdentify the source word or short source phrase represented by the selected target word, then suggest up to 5 natural alternatives that are drop-in replacements for exactly this selected span. Return JSON only: {\\"sourceTerm\\":\\"...\\",\\"partOfSpeech\\":\\"...\\",\\"meaning\\":\\"short plain-English meaning in context\\",\\"alternatives\\":[{\\"term\\":\\"...\\",\\"note\\":\\"short nuance\\"}]}\`\n      let instructions = \`You are a bilingual editor refining a translation into \${target}. Return valid JSON only.\`\n      if (isGermanTarget(target)) instructions += \` \${germanVariantRule(target)} \${registerRules()}\`\n      if (target === 'Hinglish') instructions += ' Hinglish must be natural Hindi written only in Roman/Latin letters, never Devanagari.'\n      const parsed = parseJson(await callLuna(prompt, instructions)) || {}\n      const alternatives = Array.isArray(parsed.alternatives) ? parsed.alternatives.filter(x => x?.term && String(x.term).toLowerCase() !== word.toLowerCase()).slice(0, 5) : []\n      setSelected(prev => prev ? { ...prev, sourceTerm: String(parsed.sourceTerm || '').trim(), partOfSpeech: String(parsed.partOfSpeech || '').trim(), meaning: String(parsed.meaning || '').trim(), alternatives } : prev)\n    } catch (err) { setSelected(prev => prev ? { ...prev, error: err.message || 'Could not load alternatives' } : prev) }\n    finally { setSuggestLoading(false) }\n  }`

const newInspect = `  const inspectWord = async (word, start, end) => {\n    if (!output || suggestLoading) return\n    const cacheKey = [target, register, output, start, end].join('::')\n    const cached = refinementCacheRef.current.get(cacheKey)\n    if (cached) {\n      setSelected({ word, start, end, ...cached })\n      return\n    }\n\n    setSelected({ word, start, end, sourceTerm: '', partOfSpeech: '', meaning: '', alternatives: [] })\n    setSuggestLoading(true)\n    try {\n      const ratio = output.length ? start / output.length : 0\n      const estimatedSourcePos = Math.max(0, Math.min(input.length, Math.round(input.length * ratio)))\n      const sourceStart = Math.max(0, estimatedSourcePos - 1400)\n      const sourceEnd = Math.min(input.length, estimatedSourcePos + 1400)\n      const targetStart = Math.max(0, start - 500)\n      const targetEnd = Math.min(output.length, end + 500)\n      const sourceContext = input.slice(sourceStart, sourceEnd)\n      const targetContext = output.slice(targetStart, targetEnd)\n      const prompt = \`SOURCE CONTEXT:\\n\${sourceContext}\\n\\nTARGET CONTEXT:\\n\${targetContext}\\n\\nSELECTED TARGET WORD:\\n\${word}\\n\\nIdentify the exact source word or shortest source phrase represented by the selected target word. Suggest up to 4 fluent, context-correct alternatives that could replace exactly this word without breaking the sentence. Keep notes extremely short. Return JSON only: {\\"sourceTerm\\":\\"...\\",\\"partOfSpeech\\":\\"...\\",\\"meaning\\":\\"short meaning in context\\",\\"alternatives\\":[{\\"term\\":\\"...\\",\\"note\\":\\"...\\"}]}\`\n      let instructions = \`You are a fast bilingual editor refining a translation into \${target}. Accuracy and speed both matter. Return valid JSON only. Do not explain your reasoning.\`\n      if (isGermanTarget(target)) instructions += \` \${germanVariantRule(target)} \${registerRules()}\`\n      if (target === 'Hinglish') instructions += ' Hinglish must be natural Hindi written only in Roman/Latin letters, never Devanagari.'\n      const parsed = parseJson(await callLuna(prompt, instructions)) || {}\n      const alternatives = Array.isArray(parsed.alternatives) ? parsed.alternatives.filter(x => x?.term && String(x.term).toLowerCase() !== word.toLowerCase()).slice(0, 4) : []\n      const result = {\n        sourceTerm: String(parsed.sourceTerm || '').trim(),\n        partOfSpeech: String(parsed.partOfSpeech || '').trim(),\n        meaning: String(parsed.meaning || '').trim(),\n        alternatives,\n      }\n      refinementCacheRef.current.set(cacheKey, result)\n      if (refinementCacheRef.current.size > 60) refinementCacheRef.current.delete(refinementCacheRef.current.keys().next().value)\n      setSelected(prev => prev ? { ...prev, ...result } : prev)\n    } catch (err) { setSelected(prev => prev ? { ...prev, error: err.message || 'Could not load alternatives' } : prev) }\n    finally { setSuggestLoading(false) }\n  }`

app = replaceOrThrow(app, oldInspect, newInspect, 'word refinement flow')

api = replaceOrThrow(
  api,
  `  const isAnaTranslation = rawInstructions.includes('premium translation engine')\n  const isWordRefinement = rawInstructions.includes('bilingual editor refining a translation')\n  const preserveLayout = isAnaTranslation && /[\\r\\n]/.test(text)\n\n  const model = (isAnaBriefing || isTalkDebrief) ? 'gpt-5.6-luna' : isTalkTurn ? 'gpt-5.6-sol' : 'gpt-5.6-luna'\n  const reasoningEffort = (isAnaBriefing || isTalkDebrief) ? 'low' : isTalkTurn ? 'medium' : 'medium'\n  const deadlineMs = isAnaBriefing ? 5500 : isTalkDebrief ? 6500 : isTalkTurn ? 15000 : 20000`,
  `  const isAnaTranslation = rawInstructions.includes('premium translation engine')\n  const isWordRefinement = rawInstructions.includes('bilingual editor refining a translation')\n  const isLanguageDetection = rawInstructions.includes("Ana's language detector")\n  const preserveLayout = isAnaTranslation && /[\\r\\n]/.test(text)\n\n  // Translation is Ana's backbone: use the stronger model for final translation quality.\n  // Refinement and language detection stay on the fast route with low reasoning.\n  const model = isTalkTurn || isAnaTranslation ? 'gpt-5.6-sol' : 'gpt-5.6-luna'\n  const reasoningEffort = isWordRefinement || isLanguageDetection || isAnaBriefing || isTalkDebrief ? 'low' : 'medium'\n  const deadlineMs = isWordRefinement ? 6000 : isLanguageDetection ? 4500 : isAnaBriefing ? 5500 : isTalkDebrief ? 6500 : isTalkTurn ? 15000 : isAnaTranslation ? 18000 : 20000`,
  'model routing',
)

api = replaceOrThrow(
  api,
  `    if (isAnaBriefing) body.max_output_tokens = 900\n    if (isTalkDebrief) body.max_output_tokens = 700\n    if (isWordRefinement) body.max_output_tokens = 650`,
  `    if (isAnaBriefing) body.max_output_tokens = 900\n    if (isTalkDebrief) body.max_output_tokens = 700\n    if (isLanguageDetection) body.max_output_tokens = 90\n    if (isWordRefinement) body.max_output_tokens = 280`,
  'output token limits',
)

fs.writeFileSync(appPath, app)
fs.writeFileSync(apiPath, api)
console.log('Translation quality and refinement speed upgraded')
